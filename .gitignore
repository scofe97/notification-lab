# OS
.DS_Store

# IDE
.idea/
*.iml

# 백업 잔여물
*.zip

# Gradle 산출물 (각 앱 하위 .gitignore와 이중 방어)
apps/*/build/
apps/*/.gradle/

# 세션 인계용 임시 메모 (작업 끝나면 비우는 파일)
.omc/

# 외부 helm 차트 사본 (helm pull 로 재현 — 아래 명령은 실험 README 참조)
**/_chart/
