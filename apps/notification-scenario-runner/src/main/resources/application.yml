server:
  port: 8094

spring:
  application:
    name: notification-scenario-runner
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP:localhost:9192}

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.practice.scenario: INFO
