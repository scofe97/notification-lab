# 빌드 컨텍스트에서 제외 — 호스트 빌드 산출물이 컨테이너 빌드에 섞이지 않게 한다.
#
# ⚠️ build/ 를 빼지 않으면 호스트에서 만든 jar 이 COPY src 이전에 이미지로 들어가
#    "고쳤는데 반영이 안 되는" 상태가 생긴다. .gradle/ 은 호스트 절대경로가 박힌
#    캐시라 컨테이너 안에서는 오히려 방해가 된다.
build/
.gradle/

.idea/
*.iml
*.log
*.mv.db
*.trace.db

# 런타임 산출물 (UC-5 아카이브)
archive/

.DS_Store
